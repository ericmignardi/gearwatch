services:
  app:
    image: postgres:16
    container_name: app
    restart: unless-stopped
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - app_data:/var/lib/postgresql/data

volumes:
  app_data:
